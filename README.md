# EHEAlRAM
this is a silly project that was coded with Arduino. It's an annoying alarm clock that is basically ragebait
The button with the red wire changes to the positions of the hours and minutes.
While the button with the blue wire sets the alarm time
